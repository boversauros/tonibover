---
import Icon from '@components/ui/Icon.astro';

const sections = [
  { id: '20', title: 'UN PROCÉS TRANS A LA MADURESA' },
  { id: '40', title: "UN PROCÉS TRANS DE L'ANALÒGIC AL DIGITAL" },
  { id: '60', title: 'UN PROCÉS TRANS LABORAL' },
];

interface Props {
  currentSection: string;
}

const { currentSection } = Astro.props;
const currentIndex = sections.findIndex((section) => section.id === currentSection);
const prevSection = currentIndex > 0 ? sections[currentIndex - 1] : null;
const nextSection = currentIndex < sections.length - 1 ? sections[currentIndex + 1] : null;
---

<nav class="mt-16 border-t border-default/50 pt-8">
  <div class="flex justify-between items-center">
    {
      prevSection && (
        <a
          href={`/preambul/${prevSection.id}`}
          class="group flex items-center text-muted hover:text-secondary transition-colors"
        >
          <Icon
            name="arrow-left"
            size="5"
            class="mr-2 transform group-hover:-translate-x-1 transition-transform"
          />
          <span>
            <span class="block text-sm text-subtle">Anterior</span>
            <span class="block">{prevSection.title}</span>
          </span>
        </a>
      )
    }

    {
      nextSection && (
        <a
          href={`/preambul/${nextSection.id}`}
          class="group flex items-center text-right text-muted hover:text-secondary transition-colors ml-auto"
        >
          <span>
            <span class="block text-sm text-subtle">Següent</span>
            <span class="block">{nextSection.title}</span>
          </span>
          <Icon
            name="arrow-right"
            size="5"
            class="ml-2 transform group-hover:translate-x-1 transition-transform"
          />
        </a>
      )
    }
  </div>
</nav>
